<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vogproject.mktfy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".general.network.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MKTFY">
        <activity android:name=".ui.listings.MyListings"></activity>
        <activity android:name=".ui.navhost.MainActivity3" />
        <activity android:name=".ui.navhost.MainActivity2" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.navhost.MainActivity" />
        <activity android:name=".ui.home.HomeActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>